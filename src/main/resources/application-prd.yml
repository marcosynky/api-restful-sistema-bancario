spring:
  datasource:
    url: jdbc:postgresql://web-production-9565.up.railway.app:5432/railway?user=${PGUSER}&password=${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
